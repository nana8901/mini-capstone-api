@products.map do |product|
  {
    id: product.id,
    name: product.name,
    price: product.price,
    images: @product.image,
    description: product.description,
    stock: product.stock
  }
end
